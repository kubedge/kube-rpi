---
kind: Pod
apiVersion: v1
metadata:
  name: kube-node03-vol01-pod
spec:
  volumes:
    - name: kube-node03-pv-storage
      persistentVolumeClaim:
       claimName: kube-node03-vol01-claim
  containers:
    - name: kube-node03-pv-container
      image: nginx
      ports:
        - containerPort: 80
          name: "http-server"
      volumeMounts:
        - mountPath: "/usr/share/nginx/html"
          name: kube-node03-pv-storage
...
---
kind: Pod
apiVersion: v1
metadata:
  name: kube-node03-vol02-pod
spec:
  volumes:
    - name: kube-node03-pv-storage
      persistentVolumeClaim:
       claimName: kube-node03-vol02-claim
  containers:
    - name: kube-node03-pv-container
      image: nginx
      ports:
        - containerPort: 80
          name: "http-server"
      volumeMounts:
        - mountPath: "/usr/share/nginx/html"
          name: kube-node03-pv-storage
...
---
kind: Pod
apiVersion: v1
metadata:
  name: kube-node03-vol03-pod
spec:
  volumes:
    - name: kube-node03-pv-storage
      persistentVolumeClaim:
       claimName: kube-node03-vol03-claim
  containers:
    - name: kube-node03-pv-container
      image: nginx
      ports:
        - containerPort: 80
          name: "http-server"
      volumeMounts:
        - mountPath: "/usr/share/nginx/html"
          name: kube-node03-pv-storage
...
---
kind: Pod
apiVersion: v1
metadata:
  name: kube-node03-vol04-pod
spec:
  volumes:
    - name: kube-node03-pv-storage
      persistentVolumeClaim:
       claimName: kube-node03-vol04-claim
  containers:
    - name: kube-node03-pv-container
      image: nginx
      ports:
        - containerPort: 80
          name: "http-server"
      volumeMounts:
        - mountPath: "/usr/share/nginx/html"
          name: kube-node03-pv-storage
...
---
kind: Pod
apiVersion: v1
metadata:
  name: kube-node03-vol05-pod
spec:
  volumes:
    - name: kube-node03-pv-storage
      persistentVolumeClaim:
       claimName: kube-node03-vol05-claim
  containers:
    - name: kube-node03-pv-container
      image: nginx
      ports:
        - containerPort: 80
          name: "http-server"
      volumeMounts:
        - mountPath: "/usr/share/nginx/html"
          name: kube-node03-pv-storage
...
---
kind: Pod
apiVersion: v1
metadata:
  name: kube-node03-vol06-pod
spec:
  volumes:
    - name: kube-node03-pv-storage
      persistentVolumeClaim:
       claimName: kube-node03-vol06-claim
  containers:
    - name: kube-node03-pv-container
      image: nginx
      ports:
        - containerPort: 80
          name: "http-server"
      volumeMounts:
        - mountPath: "/usr/share/nginx/html"
          name: kube-node03-pv-storage
...
---
kind: Pod
apiVersion: v1
metadata:
  name: kube-node03-vol07-pod
spec:
  volumes:
    - name: kube-node03-pv-storage
      persistentVolumeClaim:
       claimName: kube-node03-vol07-claim
  containers:
    - name: kube-node03-pv-container
      image: nginx
      ports:
        - containerPort: 80
          name: "http-server"
      volumeMounts:
        - mountPath: "/usr/share/nginx/html"
          name: kube-node03-pv-storage
...
